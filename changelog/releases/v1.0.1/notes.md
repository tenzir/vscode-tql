# VS Code TQL v1.0.1

This release makes the TQL TextMate grammar available to consumers that import the package directly.

## 🚀 Features

### Importable TextMate grammar

Consumers can now import the package directly to access the TQL TextMate grammar as a JavaScript object.

_By @dit7ya in #10._
